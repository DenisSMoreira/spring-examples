'use strict';
angular.module('alerts').factory('AlertsService',
        [
            '$http', '$auth',
            function AlertsService($http, $auth) {

                return AlertsService;
            }]);
